// Optional JS if you want to change the message dynamically or control speed
document.addEventListener("DOMContentLoaded", () => {
  const banner = document.getElementById("bannerText");
  // You could update the banner content dynamically like this:
  // banner.textContent = "New banner message goes here!";
});
